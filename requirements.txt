# Core API client
openai>=1.0.0

# SenseVoice / ASR-SER stack
funasr>=1.3.1
modelscope>=1.13.0
numpy<=1.26.4

# Qwen3-TTS runtime
qwen-tts>=0.1.0
soundfile>=0.12.1
librosa>=0.10.1

# Optional local UI / service helpers
fastapi>=0.111.1
gradio>=4.0.0

# IMPORTANT:
# Install torch/torchaudio separately based on your GPU/CUDA.
# For RTX 5060 (sm_120), prefer nightly cu128 in your sensevoice env:
#   pip install --pre torch torchaudio --index-url https://download.pytorch.org/whl/nightly/cu128
